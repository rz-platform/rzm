{
  "name": "razam-front",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "parcel build stylesheets/* javascript/*  --no-source-maps --out-dir ../public/dist --no-cache",
    "watch": "parcel watch stylesheets/* javascript/*"
  },
  "author": "Eugene Bosiakov",
  "license": "Apache 2.0",
  "devDependencies": {
    "cssnano": "^4.1.10",
    "parcel-bundler": "^1.12.4",
    "sass": "^1.26.5"
  },
  "dependencies": {
    "codemirror": "^5.50.2",
    "milligram": "^1.3.0"
  },
  "sass": {
    "includePaths": ["./node_modules"]
  }
}
